<Window x:Class="Automated_Employee_Attendance_System.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Automated_Employee_Attendance_System"
        mc:Ignorable="d"
        RenderOptions.BitmapScalingMode="HighQuality"
        Foreground="{DynamicResource Forground}"
        AllowsTransparency="True"
        FontFamily="{StaticResource CustomFont}"
        WindowStyle="None"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Title="LoginWindow" Height="500"  Width="800">


    <Grid>

        <Border CornerRadius="15" Background="{DynamicResource ContBackground}">
            <Grid>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="400" />
                </Grid.ColumnDefinitions>



                <StackPanel HorizontalAlignment="Center" Margin="17,0,0,0" VerticalAlignment="Center">
                    <Image x:Name="MyGifImage" Width="380"
                    HorizontalAlignment="Center" VerticalAlignment="Center"
                    RenderOptions.BitmapScalingMode="HighQuality"
                    SnapsToDevicePixels="True" />
                </StackPanel>

                                          
                

                <Border Grid.Column="1" Width="360"
                        Margin="15"
      Padding="10"
      Background="{DynamicResource Background}"
      CornerRadius="16"
      VerticalAlignment="Center"
      >
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="25"
                        ShadowDepth="0"
                        Opacity="0.2"/>
                    </Border.Effect>

                  

                    <StackPanel Margin="15">

                        <StackPanel HorizontalAlignment="Right">
                            <Button Click="Close_Click" Content="X" Style="{DynamicResource ActionButton}" Width="25" />
                        </StackPanel>

                        <!-- logo -->
                        <Border Width="64" Height="64"
              Background="{DynamicResource ContBackground}"
              CornerRadius="22"
              HorizontalAlignment="Center"
              Margin="0,0,0,16">
                            <Image
                                Source="/UI/logo.png"
                                Width="64"
                                Height="64"/>
                        </Border>

                        <!-- Title -->
                        <TextBlock Text="Admin Portal"
                 FontSize="22"
                 FontWeight="Bold"
                 HorizontalAlignment="Center"/>

                        <TextBlock Text="Secure access for employee management."
                 FontSize="12"
                 Foreground="Gray"
                 HorizontalAlignment="Center"
                 Margin="0,6,0,25"/>

                        <!-- Email -->
                        <TextBlock Text="User Name"
                 FontSize="12"
                 FontWeight="SemiBold"
                 Margin="0,0,0,6"/>

                        <TextBox x:Name="UserName"  Style="{DynamicResource TextBoxStyle}" />

                        <!-- Password -->
                        <TextBlock Text="Password"
                 FontSize="12"
                 FontWeight="SemiBold"
                 Margin="0,18,0,6"/>

                        <PasswordBox x:Name="Password" 
                    />

                        <!-- Remember + Forgot -->
                        <Grid Margin="0,14,0,18">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>



                            <TextBlock Text="Forgot Password?"
                     Grid.Column="1"
                     Foreground="#1A73E8"
                     FontSize="11"
                     HorizontalAlignment="Right"
                     Cursor="Hand"/>
                        </Grid>

                        <!-- Login Button -->
                        <Button Content="Login" 
           
                                Click="Login_Click"
             Style="{DynamicResource ActionButton}"
                                Width="150"
                                IsDefault="True">

                        </Button>

                        <!-- Status -->
                        <StackPanel Orientation="Horizontal"
                  HorizontalAlignment="Center"
                  Margin="0,18,0,0">
                            <Ellipse Width="6" Height="6" Fill="Green" Margin="0,0,6,0"/>
                            <TextBlock Text="System Operational"
                     FontSize="10"
                     Foreground="Gray"/>
                        </StackPanel>

                    </StackPanel>
                </Border>

            </Grid>
        </Border>
       
    </Grid>
   
</Window>
